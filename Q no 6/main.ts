//Q no 6
let personName="Babar Azam"
console.log(`\n\t${personName}\n\t`);// \t is used for spaces & \n is used for line spaces
let user="      azam khan    "
console.log(`${user.trim()}`);//.trim is used to remove the spaces

















